<Window x:Class="WpfCube.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:idx="clr-namespace:IndirectX.Helper.Wpf;assembly=IndirectX.Helper.Wpf"
        xmlns:vm="clr-namespace:WpfCube.Models"
        ContentRendered="Window_ContentRendered"
        Closing="Window_Closing"
        mc:Ignorable="d"
        Title="WpfLightedCube"
        ResizeMode="CanMinimize"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        SizeToContent="WidthAndHeight"
        d:DataContext="{d:DesignData Type={x:Type vm:ViewModel}}">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:FloatViewModel}" x:Key="ParameterTemplate">
            <StackPanel Orientation="Horizontal">
                <TextBlock Width="80" Text="{Binding Name, Mode=OneWay}" />
                <TextBox Width="40" Text="{Binding Value, Mode=TwoWay, StringFormat=0.00}" />
                <Slider Width="100"
                        Value="{Binding Value, Mode=TwoWay}"
                        Minimum="{Binding Min, Mode=OneTime}"
                        Maximum="{Binding Max, Mode=OneTime}"
                        IsSnapToTickEnabled="True"
                        TickFrequency="0.01" />
            </StackPanel>
        </DataTemplate>
        <Style TargetType="{x:Type ItemsControl}" x:Key="VirtualizedItemsControlStyle">
            <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="ScrollViewer.PanningMode" Value="VerticalOnly" />
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel />
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ItemsControl}">
                        <Border BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Background="{TemplateBinding Background}"
                                SnapsToDevicePixels="True">
                            <ScrollViewer Padding="{TemplateBinding Control.Padding}">
                                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <idx:DirectXHost Height="512" Width="512" x:Name="drawSurface" />
        <ItemsControl Grid.Column="1"
                      Margin="5,2"
                      Width="220"
                      ItemsSource="{Binding Path=Parameters, Mode=OneWay}"
                      ItemTemplate="{StaticResource ParameterTemplate}"
                      Style="{StaticResource VirtualizedItemsControlStyle}" />
    </Grid>
</Window>
